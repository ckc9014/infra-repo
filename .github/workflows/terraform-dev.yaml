name: Terraform Dev

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action (apply or destroy)'
        required: true
        type: choice
        options:
          - apply
          - destroy

permissions:
  id-token: write   # Required for OIDC authentication
  contents: read 

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform and AWS
        uses: ./.github/actions/setup-terraform-oidc
        with:
          aws-role-arn: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
          working-directory: ./terraform/infrastructure
          tf-state-bucket: ${{ vars.TF_STATE_BUCKET }}
          tf-state-key: 'infra/dev/terraform.tfstate' 

      - name: Terraform ${{ github.event.inputs.action }}
        working-directory: ./terraform/infrastructure
        run: |
          if [[ "${{ github.event.inputs.action }}" == "apply" ]]; then
            terraform apply -auto-approve -var-file="envs/dev.tfvars" 
          else
            terraform destroy -auto-approve -var-file="envs/dev.tfvars" 
          fi
      # - name: Get EKS cluster name and endpoint
      #   if: github.event.inputs.action == 'apply'
      #   id: get_cluster
      #   working-directory: ./terraform
      #   run: |
      #     echo "cluster_name=$(terraform output -raw cluster_name)" >> $GITHUB_OUTPUT
      #     echo "cluster_endpoint=$(terraform output -raw cluster_endpoint)" >> $GITHUB_OUTPUT
          


      # - name: Configure kubectl for EKS
      #   if: github.event.inputs.action == 'apply'
      #   run: |
      #     aws eks update-kubeconfig --region ${{ vars.AWS_REGION }} --name ${{ steps.get_cluster.outputs.cluster_name }}


      # - name: Apply ArgoCD root Applications
      #   run: |
      #     kubectl apply -f argocd/apps-root.yaml
      #     kubectl apply -f argocd/platform-root.yaml

      
      # - name: Get Karpenter node role name
      #   if: github.event.inputs.action == 'apply'
      #   id: get_karpenter_role
      #   working-directory: ./terraform
      #   run: |
      #    echo "role_name=$(terraform output -raw karpenter_node_role_name)" >> $GITHUB_OUTPUT

      # - name: Apply Karpenter NodePool and EC2NodeClass
      #   run: |
      #      envsubst < karpenter/ec2nodeclass.yaml.tmpl > karpenter/ec2nodeclass.yaml
      #      cp karpenter/nodepool.yaml.tmpl karpenter/nodepool.yaml
      #      kubectl apply -f karpenter/nodepool.yaml
      #      kubectl apply -f karpenter/ec2nodeclass.yaml
      
      #   env:
      #     cluster_name: ${{ steps.get_cluster.outputs.cluster_name }}
      #     karpenter_node_role: ${{ steps.get_karpenter_role.outputs.role_name }}

      # - name: Create ARC GitHub App secret in cluster
      #   if: github.event.inputs.action == 'apply'
      #   run: |
      #     kubectl create namespace arc-systems --dry-run=client -o yaml | kubectl apply -f -
      #     kubectl create secret generic github-app-auth \
      #       -n arc-systems \
      #       --dry-run=client \
      #       --from-literal=cluster_name=${{ steps.get_cluster.outputs.cluster_name }} \
      #       --from-literal=cluster_endpoint=${{ steps.get_cluster.outputs.cluster_endpoint }} \
      #       --from-literal=github_app_id=${{ secrets.ARC_APP_ID }} \
      #       --from-literal=github_app_installation_id=${{ secrets.ARC_APP_INSTALLATION_ID }} \
      #       --from-literal=github_app_private_key="${{ secrets.ARC_APP_PRIVATE_KEY }}" \
      #       -o yaml | kubectl apply -f -


           
      # - name: Wait for core components
      #   if: github.event.inputs.action == 'apply'
      #   run: |
      #     kubectl wait --for=condition=available --timeout=300s deployment/argocd-server -n argocd || echo "ArgoCD not ready yet"
      #     kubectl wait --for=condition=available --timeout=180s deployment/karpenter -n kube-system || echo "Karpenter not ready yet"

      # - name: Check ArgoCD Applications
      #   if: github.event.inputs.action == 'apply'
      #   run: kubectl get applications -n argocd -o wide

      # - name: Check pod status for key namespaces
      #   if: github.event.inputs.action == 'apply'
      #   run: |
      #     echo "=== GPU Operator ==="
      #     kubectl get pods -n gpu-operator 2>/dev/null || echo "installation failed"
      #     echo "=== Prometheus stack ==="
      #     kubectl get pods -n monitoring 2>/dev/null || echo "installation failed"
      #     echo "=== ARC controller ==="
      #     kubectl get pods -n arc-systems 2>/dev/null || echo "installation failed"
      #     echo "=== Argo Rollouts ==="
      #     kubectl get pods -n argo-rollouts 2>/dev/null || echo "installation failed"   