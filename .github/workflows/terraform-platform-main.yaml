name: Apply Platform (ArgoCD, Karpenter, etc.)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment (dev/prod)'
        required: true
        default: 'dev'
        type: choice
        options: ['dev', 'prod']
  push:
    branches: [ main ]
    paths:
      - 'terraform/platform/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'terraform/platform/**'

permissions:
  id-token: write
  contents: read

jobs:
  env:
    uses: ./.github/workflows/determine-environment.yaml

  plan:
    needs: env
    uses: ./.github/workflows/terraform-plan.yaml
    with:
      working-directory: ./terraform/platform
      aws-role-arn: ${{ vars.AWS_ROLE_ARN }}
      aws-region: ${{ vars.AWS_REGION }}
      tf-state-bucket: ${{ vars.TF_STATE_BUCKET }}
      tf-state-key: platform/${{ needs.env.outputs.environment }}/terraform.tfstate
      environment: ${{ needs.env.outputs.environment }}

  apply:
    needs: [env, plan]
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch')
    uses: ./.github/workflows/terraform-apply.yaml
    with:
      working-directory: ./terraform/platform
      aws-role-arn: ${{ vars.AWS_ROLE_ARN }}
      aws-region: ${{ vars.AWS_REGION }}
      tf-state-bucket: ${{ vars.TF_STATE_BUCKET }}
      tf-state-key: platform/${{ needs.env.outputs.environment }}/terraform.tfstate
      
  create-arc-secret:
    runs-on: ubuntu-latest
    needs: apply
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform (to read infra outputs)
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.11.0'

      - name: Get cluster name from infrastructure state
        id: get_cluster
        working-directory: ./terraform/infrastructure
        run: |
          terraform init -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" -backend-config="key=infra/${{ needs.env.outputs.environment }}/terraform.tfstate"
          echo "cluster_name=$(terraform output -raw cluster_name)" >> $GITHUB_OUTPUT
          echo "cluster_endpoint=$(terraform output -raw cluster_endpoint)" >> $GITHUB_OUTPUT

      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig --region ${{ vars.AWS_REGION }} --name ${{ steps.get_cluster.outputs.cluster_name }}

      - name: Create ARC GitHub App secret in cluster
        run: |
          kubectl create namespace arc-systems --dry-run=client -o yaml | kubectl apply -f -
          kubectl create secret generic github-app-auth \
            -n arc-systems \
            --dry-run=client \
            --from-literal=cluster_name=${{ steps.get_cluster.outputs.cluster_name }} \
            --from-literal=cluster_endpoint=${{ steps.get_cluster.outputs.cluster_endpoint }} \
            --from-literal=github_app_id=${{ secrets.ARC_APP_ID }} \
            --from-literal=github_app_installation_id=${{ secrets.ARC_APP_INSTALLATION_ID }} \
            --from-literal=github_app_private_key="${{ secrets.ARC_APP_PRIVATE_KEY }}" \
            -o yaml | kubectl apply -f -
    